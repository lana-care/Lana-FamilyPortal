# App environment — controls secure cookie flag (set to "production" in prod)
NODE_ENV=development

# The family portal does NOT use Supabase directly — it is fully token-scoped
# via the NestJS backend API, so no Supabase keys are required here.

# NestJS API (family portal data + feedback)
NUXT_PUBLIC_API_URL=http://localhost:4000
